<%= form_with(model: tarefa) do |form| %>
  <% if tarefa.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tarefa.errors.count, "error") %> prohibited this tarefa from being saved:</h2>

      <ul>
        <% tarefa.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :titulo, style: "display: block" %>
    <%= form.text_field :titulo %>
  </div>

  <div>
    <%= form.label :descricao, style: "display: block" %>
    <%= form.text_area :descricao, id: :tarefa_descricao %>
  </div>
  <div class="field">
    <%# form.label :checklist_items %>
    <%# form.collection_check_boxes :checklist_items, ['Item 1','Item 2','Item 3'], :itself, :to_s %>
  </div>
  <%#= form.hidden_field :checklist_id, value: @checklist.id %>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
